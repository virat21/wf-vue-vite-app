# Vue + Vite starter project

## How to run
- Use `npm run dev` to run development server that watches for code changes and serves using the Webflow CLI
- Use `npm run build` to statically output your project (look for `bundle.zip`)